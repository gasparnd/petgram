import styled from 'styled-components'

export const Button = styled.button`
  display: flex;
  background-color: transparent;
  border: transparent;
  align-items: center;
  padding: .7rem 0 1rem;
  & svg {
  	margin-right: 4px;
  }
`